# This file is managed with Chef. For changes to persist, edit http_platform/templates/dh_config.erb
# The only purpose of this file is to signal the DH creation script

# <%= node['http_platform']['cert']['dh_param']['bits'] %>
