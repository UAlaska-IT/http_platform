# This file is managed with Chef. For changes to persist, edit secure_apache/templates/dh_config.erb
# The only purpose of this file is to signal the DH creation script

# <%= node['secure_apache']['cert']['dh_param']['bits'] %>
